<div class="blue-grey darken-3 s-padding">
<div class="row">
  <div class="col s8 offset-s2 blue-grey-text text-lighten-5">
  あなたの素敵な体験を人々と共有し、更なる旅の始まりを探そう！多くの体験と感動を...</div>
  </div>
</div>

<div class="container m-padding">
  <div class="contents_left">
     <h4>Travel photo</h4>
      <div class="box">
        <% @matelpages.each do |matelpage| %>
          <!-- <a href="/matelpages/<%= matelpage.user_id %>/comments/new"> -->
            <span class="content_post" style="background-image: url(<%= matelpage.image %>);">
  <span class="name">
    <a href="/users/<%= matelpage.user_id %>">
      <span>投稿者</span><%= matelpage.user.nickname %>
    </a>
  </span>
            </span>
            <%= simple_format(matelpage.country) %>
            <%= simple_format(matelpage.text) %>
         <!--  </a> -->
        <% end %>
      </div>
  </div>
</div>
   <div class="contents_right">
    <h6>New</h6>
     <div class="box2">
        <% @matelpages.each do |matelpage| %>
            <span class="content_post2" style="background-image: url(<%= matelpage.image %>);">
            </span>
          <% end %>
      </div>
  </div>


